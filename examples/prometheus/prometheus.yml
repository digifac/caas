global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "caas"
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets: ["caas:8000"]
        labels:
          service: caas
          environment: local
